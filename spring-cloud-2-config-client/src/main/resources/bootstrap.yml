spring:
  cloud:
    config:
      name: xianzhan
      profile: dev
      label: master
#      uri: http://localhost:8688
      # 使用服务发现
      discovery:
        enabled: true
        service-id: config-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka